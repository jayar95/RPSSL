<div class="nav-container">
    <h3>Welcome, {{ player.name }}</h3>

    <div class="nav">
        Menu:

        <a class="main-btn" href="{{ path('game.index', {'player': player.id}) }}">
            Play
        </a>

        <a class="main-btn" href="{{ path('game.history', {'player': player.id}) }}">
            History / Statistics
        </a>
    </div>
</div>